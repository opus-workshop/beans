---
id: '84.2'
title: Atomic file writes for beans and index
slug: atomic-file-writes-for-beans-and-index
status: open
priority: 0
created_at: 2026-02-24T06:49:21.423264Z
updated_at: 2026-02-24T06:49:21.423264Z
parent: '84'
verify: rg 'fn atomic_write' src/util.rs && cargo test util::tests::test_atomic
fail_first: true
produces:
- AtomicWrites
tokens: 24062
tokens_updated: 2026-02-24T06:49:21.424837Z
---

## Problem

`bean.to_file()` and `index.save()` write directly to the target path. A crash or kill mid-write corrupts the file.

## Approach

Write to a temp file in the same directory, then `rename()` atomically.

### Files to modify
- `src/util.rs` — add `atomic_write(path, contents)` helper
- `src/bean.rs` — `to_file()` uses atomic_write
- `src/index.rs` — `save()` uses atomic_write

### Design
- Write to `{path}.tmp.{pid}` then `std::fs::rename()`
- rename() is atomic on POSIX (same filesystem guaranteed since same dir)
- Clean up temp file on error
- No new dependencies needed

### Acceptance Criteria
- [ ] `atomic_write` function exists in src/util.rs
- [ ] `bean.to_file()` uses atomic write
- [ ] `index.save()` uses atomic write
- [ ] Test: atomic_write produces correct file contents
- [ ] All existing tests pass
