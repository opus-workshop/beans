id: '65.1'
title: Add extends field and config merging
slug: add-extends-field-and-config-merging
status: closed
priority: 2
created_at: 2026-02-22T07:49:06.107596Z
updated_at: 2026-02-22T09:12:47.681669Z
description: "## Task\nAdd extends field to Config and implement config loading with inheritance.\n\n## Steps\n1. Add extends field to Config in src/config.rs:\n```rust\n#[serde(default, skip_serializing_if = \"Vec::is_empty\")]\npub extends: Vec<String>,\n```\n\n2. Add `load_with_extends` method:\n```rust\npub fn load_with_extends(beans_dir: &Path) -> Result<Self> {\n    let mut config = Self::load(beans_dir)?;\n    \n    if config.extends.is_empty() {\n        return Ok(config);\n    }\n    \n    // Resolve and load parent configs (first = lowest priority)\n    let mut seen = HashSet::new();\n    let mut stack: Vec<String> = config.extends.clone();\n    let mut parents: Vec<Config> = Vec::new();\n    \n    while let Some(path_str) = stack.pop() {\n        // Resolve ~ to home dir\n        let resolved = if path_str.starts_with(\"~/\") {\n            dirs::home_dir()\n                .ok_or_else(|| anyhow!(\"Cannot resolve home directory\"))?\n                .join(&path_str[2..])\n        } else {\n            beans_dir.parent()\n                .unwrap_or(Path::new(\".\"))\n                .join(&path_str)\n        };\n        \n        let canonical = resolved.canonicalize()\n            .with_context(|| format!(\"Cannot resolve extends path: {}\", path_str))?;\n        \n        if !seen.insert(canonical.clone()) {\n            continue; // Cycle detection\n        }\n        \n        // Load parent config from the resolved path directly (it's a YAML file, not a beans dir)\n        let contents = fs::read_to_string(&canonical)\n            .with_context(|| format!(\"Failed to read extends config: {}\", canonical.display()))?;\n        let parent: Config = serde_yaml::from_str(&contents)\n            .with_context(|| format!(\"Failed to parse extends config: {}\", canonical.display()))?;\n        \n        // If parent also has extends, add them to stack\n        for ext in &parent.extends {\n            stack.push(ext.clone());\n        }\n        \n        parents.push(parent);\n    }\n    \n    // Merge: parents first (lowest priority), then local config (highest)\n    // Only merge inheritable fields\n    for parent in parents.iter().rev() {\n        if config.max_tokens == default_max_tokens() {\n            config.max_tokens = parent.max_tokens;\n        }\n        if config.run.is_none() {\n            config.run = parent.run.clone();\n        }\n        // Don't merge: project, next_id (always local)\n    }\n    \n    Ok(config)\n}\n```\n\n3. In src/main.rs, replace `Config::load` with `Config::load_with_extends` where appropriate\n\n## Files\n- src/config.rs (extends field, load_with_extends, merge logic)\n- src/main.rs (use load_with_extends)\n\n## Tests\n- Empty extends loads normally\n- Single extends merges fields\n- Local config overrides extended config\n- Circular extends detected and skipped\n- Missing extends file errors with helpful message\n- Recursive extends (A extends B extends C)\n- project and next_id never inherited\n- ~ resolves to home directory"
closed_at: 2026-02-22T09:12:47.681669Z
parent: '65'
verify: cargo test config::tests::extends
claimed_at: 2026-02-22T09:07:29.544828Z
is_archived: true
produces:
- ConfigExtends
tokens: 5476
tokens_updated: 2026-02-22T07:49:06.109599Z
