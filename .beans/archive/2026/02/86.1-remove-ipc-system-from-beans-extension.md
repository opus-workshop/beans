---
id: '86.1'
title: Remove IPC system from beans extension
slug: remove-ipc-system-from-beans-extension
status: closed
priority: 2
created_at: 2026-02-25T07:05:23.982479Z
updated_at: 2026-02-25T07:47:00.750574Z
notes: ''
closed_at: 2026-02-25T07:47:00.750574Z
is_archived: true
---

2026-02-25T07:44:03.202335+00:00
  Agent failed: Error: File not found: /Users/asher/beans/.beans/86.1-remove-ipc-system-from-beans-extension.md

  implement this bean and run bn close 86.1 when done
parent: '86'
verify: '! grep -q "bean_ask\|bean_respond\|ipc\.js\|ipc\.ts\|IPC_DIR_ENV\|BEAN_ID_ENV\|pendingAsks\|writeAsk\|pollForResponse" ~/.pi/agent/extensions/beans/index.ts && ! test -f ~/.pi/agent/extensions/beans/ipc.ts'
fail_first: true
produces:
- IPC_removed
tokens: 7317
tokens_updated: 2026-02-25T07:44:03.203586Z
---

## Task

Remove the speculative IPC system from the beans extension. No concrete use case drove it — it was vibecoded.

## What to remove

1. **Delete `~/.pi/agent/extensions/beans/ipc.ts`** entirely
2. **In `index.ts`**: remove the `bean_ask` tool registration (~lines 596-680)
3. **In `index.ts`**: remove the `bean_respond` tool registration (~lines 684-740)  
4. **In `index.ts`**: remove all IPC imports from ipc.js (line ~26-28)
5. **In `index.ts`**: remove `activeIpcDirs` map and `pendingAsks` map declarations (~lines 74-78)
6. **In `index.ts`**: remove IPC directory creation/cleanup in the run functions (search for `createIpcDir`, `cleanupIpcDir`, `IPC_DIR_ENV`, `BEAN_ID_ENV`)
7. **In `spawner.ts`**: remove any IPC env vars passed to child processes (`IPC_DIR_ENV`, `BEAN_ID_ENV`)
8. **In `prompt.ts`**: remove any bean_ask instructions from the agent prompt
9. **In `README.md`**: remove bean_ask/bean_respond documentation

## Approach

Work carefully — the IPC is threaded through several files. After removal, ensure `bean_run`, `bean_status`, and `bean_logs` still work (they don't depend on IPC).

## Acceptance Criteria
- ipc.ts deleted
- No references to bean_ask, bean_respond, IPC_DIR_ENV, BEAN_ID_ENV, pendingAsks in index.ts
- bean_run, bean_status, bean_logs tools still registered
