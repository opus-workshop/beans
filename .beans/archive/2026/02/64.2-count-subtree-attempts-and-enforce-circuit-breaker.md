id: '64.2'
title: Count subtree attempts and enforce circuit breaker
slug: count-subtree-attempts-and-enforce-circuit-breaker
status: closed
priority: 2
created_at: 2026-02-22T07:48:49.715568Z
updated_at: 2026-02-22T09:07:24.401413Z
description: "## Task\nCount total verify attempts across a bean's subtree and refuse retry when max_loops is exceeded.\n\n## Steps\n1. In src/graph.rs, add function to count subtree attempts:\n```rust\n/// Count total verify attempts across all descendants of a bean.\n/// Includes the bean itself.\npub fn count_subtree_attempts(beans_dir: &Path, root_id: &str) -> Result<u32> {\n    let index = Index::build(beans_dir)?;\n    let mut total = 0;\n    let mut stack = vec![root_id.to_string()];\n    \n    while let Some(id) = stack.pop() {\n        if let Some(bean) = index.beans.iter().find(|b| b.id == id) {\n            total += bean.attempts;\n            // Find children\n            for child in index.beans.iter().filter(|b| b.parent.as_deref() == Some(&id)) {\n                stack.push(child.id.clone());\n            }\n        }\n    }\n    Ok(total)\n}\n```\n\n2. In src/commands/close.rs, in the verify failure path, before allowing retry:\n   - Find the root parent (walk up parent chain)\n   - Count subtree attempts\n   - If total >= max_loops, add 'circuit-breaker' label, set P0 priority, print warning\n   - Do NOT auto-retry — escalate instead\n\n3. Logic for finding root parent:\n```rust\nfn find_root_parent(beans_dir: &Path, bean: &Bean) -> Result<String> {\n    let mut current_id = bean.id.clone();\n    loop {\n        let path = find_bean_file(beans_dir, &current_id)?;\n        let b = Bean::from_file(&path)?;\n        match b.parent {\n            Some(parent_id) => current_id = parent_id,\n            None => return Ok(current_id),\n        }\n    }\n}\n```\n\n## Files\n- src/graph.rs (count_subtree_attempts function)\n- src/commands/close.rs (check circuit breaker before retry)\n\n## Tests\n- count_subtree_attempts sums all descendant attempts\n- count_subtree_attempts includes root bean's own attempts\n- Circuit breaker triggers at max_loops\n- Circuit breaker adds label and sets P0\n- max_loops of 0 disables circuit breaker\n- Per-bean max_loops overrides config\n\n## Edge Cases\n- Archived/closed beans: include their attempts in the count (they happened)\n- Bean with no parent: use its own max_loops\n- Walk-up might hit archived parent — handle gracefully"
closed_at: 2026-02-22T09:07:24.401413Z
parent: '64'
dependencies:
- '64.1'
verify: cargo test close::tests::max_loops && cargo test graph::tests::subtree_attempts
claimed_at: 2026-02-22T08:56:55.657804Z
is_archived: true
requires:
- MaxLoopsConfig
tokens: 14798
tokens_updated: 2026-02-22T07:48:49.717488Z
