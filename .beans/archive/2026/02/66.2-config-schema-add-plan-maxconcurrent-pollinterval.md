id: '66.2'
title: 'Config schema: add plan, max_concurrent, poll_interval'
slug: config-schema-add-plan-maxconcurrent-pollinterval
status: closed
priority: 2
created_at: 2026-02-23T09:49:21.626437Z
updated_at: 2026-02-23T10:07:41.394938Z
description: "Add three new fields to Config struct in src/config.rs:\n\n1. `plan` (Option<String>) — command template for planning large beans. \n   Same pattern as `run`. Uses `{id}` placeholder.\n2. `max_concurrent` (u32, default 4) — max parallel agents for bn run\n3. `poll_interval` (u32, default 30) — seconds between polls in --watch mode\n\nAll three should have serde defaults, round-trip through YAML, and be\ninheritable via extends (same merge pattern as run/max_tokens).\n\n## Files\n- src/config.rs (modify)\n\n## Context\n\nCurrent Config struct:\n```rust\npub struct Config {\n    pub project: String,\n    pub next_id: u32,\n    pub auto_close_parent: bool,  // default true\n    pub max_tokens: u32,          // default 30000\n    pub run: Option<String>,      // command template\n    pub max_loops: u32,           // default 10\n    pub extends: Vec<String>,\n}\n```\n\nExtends merge pattern to follow:\n```rust\nif config.run.is_none() {\n    config.run = parent.run.clone();\n}\n```\n\n## Tests\n- Default values correct (plan: None, max_concurrent: 4, poll_interval: 30)\n- YAML round-trip\n- Extends inheritance for all three fields\n- Local values override parents\n- plan not serialized when None\n"
closed_at: 2026-02-23T10:07:41.394938Z
parent: '66'
verify: cargo test config
claimed_at: 2026-02-23T10:04:39.079196Z
is_archived: true
produces:
- ConfigSchema
tokens: 13
tokens_updated: 2026-02-23T09:49:21.630411Z
