id: '62.2'
title: Process on_fail in close command
slug: process-onfail-in-close-command
status: closed
priority: 2
created_at: 2026-02-22T07:48:01.286246Z
updated_at: 2026-02-22T08:47:52.690857Z
description: "## Task\nWhen verify fails in cmd_close, check bean.on_fail and take the appropriate action.\n\n## Steps\n1. In src/commands/close.rs, in the verify failure path (around line 265), after incrementing attempts and appending failure note, add on_fail processing:\n\n```rust\n// Process on_fail action\nif let Some(ref on_fail) = bean.on_fail {\n    match on_fail {\n        OnFailAction::Retry { max, delay_secs } => {\n            let max_retries = max.unwrap_or(bean.max_attempts);\n            if bean.attempts < max_retries {\n                println!(\"on_fail: will retry (attempt {}/{})\", bean.attempts, max_retries);\n                if let Some(delay) = delay_secs {\n                    println!(\"on_fail: retry delay {}s (enforced by orchestrator)\", delay);\n                }\n                // Release claim so bw/deli can pick it up\n                bean.claimed_by = None;\n                bean.claimed_at = None;\n            } else {\n                println!(\"on_fail: max retries ({}) exhausted\", max_retries);\n            }\n        }\n        OnFailAction::Escalate { priority, message } => {\n            if let Some(p) = priority {\n                let old_priority = bean.priority;\n                bean.priority = *p;\n                println!(\"on_fail: escalated priority P{} → P{}\", old_priority, p);\n            }\n            if let Some(msg) = message {\n                // Append escalation message to notes\n                let note = format!(\"\\n## Escalated — {}\\n{}\", Utc::now().format(\"%Y-%m-%dT%H:%M:%SZ\"), msg);\n                match &mut bean.notes {\n                    Some(notes) => notes.push_str(&note),\n                    None => bean.notes = Some(note),\n                }\n                println!(\"on_fail: {}\", msg);\n            }\n            // Add escalated label\n            if !bean.labels.contains(&\"escalated\".to_string()) {\n                bean.labels.push(\"escalated\".to_string());\n            }\n        }\n    }\n}\n```\n\n2. Save the bean after on_fail processing (it's already saved in the failure path)\n\n## Files\n- src/commands/close.rs (add on_fail processing to verify failure path)\n\n## Context\nThe verify failure path is around line 265 in close.rs. Currently it:\n1. Increments attempts\n2. Appends failure note to notes\n3. Saves bean\n4. Prints failure message\n5. Continues (skips closing)\n\nAdd on_fail processing AFTER step 2, BEFORE step 3 (save).\n\n## Tests\n- Retry: claim released on failure when attempts < max\n- Retry: claim NOT released when attempts >= max  \n- Retry: max defaults to bean.max_attempts when not specified\n- Escalate: priority updated\n- Escalate: message appended to notes\n- Escalate: 'escalated' label added\n- No on_fail: existing behavior unchanged"
closed_at: 2026-02-22T08:47:52.690857Z
parent: '62'
dependencies:
- '62.1'
verify: cargo test close::tests::on_fail
claimed_at: 2026-02-22T08:46:20.086208Z
is_archived: true
requires:
- OnFailAction
tokens: 12783
tokens_updated: 2026-02-22T07:48:01.287183Z
