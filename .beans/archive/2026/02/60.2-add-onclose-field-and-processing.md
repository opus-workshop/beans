id: '60.2'
title: Add on_close field and processing
slug: add-onclose-field-and-processing
status: closed
priority: 2
created_at: 2026-02-22T07:46:57.794470Z
updated_at: 2026-02-22T08:29:44.986333Z
description: "## Task\nAdd declarative on_close actions to Bean struct and process them during close.\n\n## Steps\n1. Define OnCloseAction enum in src/bean.rs:\n```rust\n#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]\n#[serde(tag = \"action\", rename_all = \"snake_case\")]\npub enum OnCloseAction {\n    Run { command: String },\n    Notify { message: String },\n}\n```\nNote: skip the `create` variant for now — it needs the full bean creation machinery. Run and Notify cover the core use case.\n\n2. Add field to Bean struct:\n```rust\n#[serde(default, skip_serializing_if = \"Vec::is_empty\")]\npub on_close: Vec<OnCloseAction>,\n```\n\n3. Add `on_close: Vec::new()` to Bean::try_new defaults\n\n4. In src/commands/close.rs, after the bean is closed and archived but before auto-close-parent:\n```rust\n// Process on_close actions\nfor action in &bean.on_close {\n    match action {\n        OnCloseAction::Run { command } => {\n            let status = std::process::Command::new(\"sh\")\n                .args([&\"-c\", command.as_str()])\n                .current_dir(project_root)\n                .status();\n            match status {\n                Ok(s) if !s.success() => eprintln!(\"on_close run command failed: {}\", command),\n                Err(e) => eprintln!(\"on_close run command error: {}\", e),\n                _ => {}\n            }\n        }\n        OnCloseAction::Notify { message } => {\n            println!(\"[bean {}] {}\", id, message);\n        }\n    }\n}\n```\n\n5. Process on_close actions AFTER PostClose hook fires (so hook can inspect the bean before actions run)\n\n## Files\n- src/bean.rs (OnCloseAction enum, on_close field on Bean, update try_new)\n- src/commands/close.rs (process on_close actions after close+archive)\n\n## Tests\n- on_close serialization round-trip (empty vec skipped, non-empty preserved)\n- on_close Run action executes command\n- on_close Notify action prints message  \n- on_close Run failure does not prevent close\n- on_close with multiple actions runs all of them\n\n## Edge Cases\n- on_close Run commands run in project root (same as verify)\n- Failures warn but don't revert the close\n- Empty on_close vec is not serialized (skip_serializing_if)"
closed_at: 2026-02-22T08:29:44.986333Z
close_reason: Implemented OnCloseAction enum and processing
parent: '60'
dependencies:
- '60.1'
verify: cargo test bean::tests::on_close && cargo test close::tests::on_close
claimed_at: 2026-02-22T08:27:38.530119Z
is_archived: true
requires:
- PostClose
tokens: 20347
tokens_updated: 2026-02-22T07:46:57.796464Z
